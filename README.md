# Dylan's Tools

个人小工具集合，部署于 Vercel。

## 工具列表

- **调仓计算器** `/tools/rebalance/` — ETF 调仓辅助，计算买卖数量（含手续费）

## 本地开发

直接用浏览器打开 `index.html` 即可，无需构建。

## 添加新工具

1. 在 `tools/` 下新建目录，如 `tools/new-tool/`
2. 放入 `index.html`
3. 在根目录 `index.html` 的工具卡片区新增入口
